import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './Share.styles';

const Share = (props) => (
 <></>
);

Share.propTypes = {
  // bla: PropTypes.string,
};

Share.defaultProps = {
  // bla: 'test',
};

export default Share;
